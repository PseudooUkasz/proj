{% extends 'base.html.twig' %}

{% block title %}Edit Categoryy{% endblock %}

{% block body %}
    <h1>Edit Categoryy</h1>

    {{ include('categoryy/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('categoryy_index') }}">back to list</a>

    {{ include('categoryy/_delete_form.html.twig') }}
{% endblock %}
